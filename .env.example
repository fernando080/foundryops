# Mock mode must be the default for the public demo.
FOUNDRY_MODE=mock

# Configure only in a private local .env; never commit real values.
FOUNDRY_API_URL=https://foundry-api-public.adaptyvbio.com/api/v1
FOUNDRY_API_TOKEN=
FOUNDRY_ORGANIZATION_ID=

# Model provider settings are intentionally unspecified until the stack ADR is approved.
LLM_PROVIDER=
LLM_API_KEY=

# Local service defaults may be added after architecture approval.
